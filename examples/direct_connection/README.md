# I²C Pressure Sensor 0–10 bar (address 0x78)

## Practical notes, pitfalls and real limitations

Этот пример посвящён недорогому китайскому **цифровому датчику давления с интерфейсом I²C**, который часто продаётся под названиями:

* `KY-3V3-IIC-10Bar`
* `I2C Pressure Sensor 3.3V 0–1MPa`
* `0–10 bar I2C pressure sensor`

На плате обычно есть наклейка вроде **KY-I2C-3V / 10Bar**.  
Во всех экземплярах, которые удалось проверить, используется **фиксированный I²C-адрес 0x78**, изменить его невозможно.

---

## ⚠️ Самое главное (кратко)

Если вы хотите, чтобы датчик **реально заработал**, нужно знать следующее:

1. ❌ Это **НЕ регистровый датчик**
2. ❌ Нельзя просто читать регистры (`readRegister`, `readStream`)
3. ✅ Это **командно-ответный I²C-датчик**
4. ✅ Перед каждым чтением **обязательно отправлять команду `0xAC`**
5. ⚠️ Реальная частота обновления данных ≈ **4–5 Гц**
6. ⚠️ Busy-бит **не работает / не используется**
7. ✅ Датчик исправен — просто документация в сети часто неверная
8. ⚠️ **ТЕМПЕРАТУРА ПОКАЗЫВАЕТ ОЧЕНЬ НЕТОЧНО** — производитель рекомендует **не использовать** значение температуры как реальный термометр

---

## Подключение

### Схема подключения к ESP32

```
ESP32          Датчик
GPIO21  ────►  SDA
GPIO22  ────►  SCL
3.3V    ────►  VCC
GND     ────►  GND
```

### Подтяжки

Рекомендуется использовать подтягивающие резисторы **4.7 кОм** на линиях SDA и SCL к 3.3V. На некоторых платах они уже установлены.

---

## 1. I²C-адрес

* **7-битный адрес:** `0x78`
* Другие адреса (`0x6D`, `0x3C` и т.п.) **не используются**
* General Call Reset (`0x00`) не поддерживается

---

## 2. Это НЕ регистровый датчик

Распространённая ошибка — считать, что у датчика есть карта регистров:

```text
REG 0x00 – pressure
REG 0x01 – config
REG 0x06 – data
```

❌ **Это неправда**

При попытке:

* читать произвольные регистры
* делать `Wire.requestFrom()` без команды

датчик будет стабильно возвращать что-то вроде:

```
04 00 00 00 00 00
```

Это **не давление**, а **статус + пустые данные**.

---

## 3. Правильный протокол работы

Датчик работает по схеме **COMMAND → DELAY → READ**.

### Шаг 1. Запуск измерения

Перед каждым чтением **обязательно** отправьте команду:

```text
WRITE 0xAC to address 0x78
```

Без этой команды:

* данные не обновляются
* давление всегда будет «замороженным»

### Шаг 2. Небольшая задержка

После команды `0xAC` нужно подождать:

* **5–15 мс** (практически оптимально ≈ 10–12 мс)

Busy-бит из документации:

* либо не реализован
* либо не отражает реальное состояние

👉 **Busy проверять не нужно**

### Шаг 3. Чтение данных

Читаем **6 байт**:

```text
READ 6 bytes from 0x78
```

Формат данных:

| Byte | Назначение                               |
| ---- | ---------------------------------------- |
| 0    | Status (обычно 0x64, можно игнорировать) |
| 1    | Pressure MSB                             |
| 2    | Pressure MID                             |
| 3    | Pressure LSB                             |
| 4    | Temperature MSB                          |
| 5    | Temperature LSB                          |

---

## 4. Давление — это 24-битное число

Сырые данные давления:

```text
rawP = (byte1 << 16) | (byte2 << 8) | byte3
```

Это **беззнаковое 24-битное значение**.

### Важно: диапазон АЦП

Согласно заводской документации:

```text
DMIN ≈ 10% = 1 677 722
DMAX ≈ 90% = 15 099 494
```

Значения ниже DMIN или выше DMAX — **некорректны**.

---

## 5. Формула пересчёта в бары

Для датчика 0–10 bar:

```text
Pbar = (PMAX − PMIN) / (DMAX − DMIN) × (rawP − DMIN) + PMIN
```

Где:

```text
PMIN = 0.0 bar
PMAX = 10.0 bar
```

---

## 6. ⚠️ Температура — очень неточная (важно!)

### Формула пересчёта температуры

Датчик возвращает 16-битное значение температуры в байтах 4-5. Формула из документации:

```text
T = (rawT / 65536) * 190 - 40
```

Где:
- `rawT` — 16-битное значение (байты 4-5)
- Диапазон: -40°C до +150°C (190°C диапазон)
- `T` — температура в градусах Цельсия

### ⚠️ Критическое предупреждение

**Температура показывает очень неточно!**

Согласно документации производителя:
- Температура среды должна быть ниже 50°C
- **Производитель рекомендует НЕ использовать значение температуры как реальный термометр**

Практические тесты на четырёх датчиках подтвердили: температура не имеет ничего общего с реальностью.

👉 **Используйте температуру только для отладки, не для реальных измерений!**

---

## 7. Реальная частота обновления (очень важно)

### ❗ Несмотря на быструю конверсию (~2 мс), датчик обновляет данные медленно

Экспериментально установлено:

* Новые данные появляются **примерно 4–5 раз в секунду**
* Увеличение частоты опроса **НЕ увеличивает количество новых измерений**
* При опросе быстрее:

  * появляются ошибки чтения
  * растёт число `fail`
  * данные дублируются

### Практический предел

```text
≈ 4–5 Hz (одно новое измерение каждые 200–250 мс)
```

Это **аппаратное ограничение датчика**.

---

## 8. Рекомендуемый рабочий алгоритм

### ✔️ Стабильный и правильный вариант

```text
каждые 200–250 мс:
  write 0xAC
  delay 10 ms
  read 6 bytes
  parse pressure
```

* Busy не проверять
* Ошибки чтения просто игнорировать
* Частота 4–5 Гц — норма

---

## 9. Частые симптомы и их объяснение

| Симптом                | Причина                  |
| ---------------------- | ------------------------ |
| Давление не меняется   | Не отправляется `0xAC`   |
| Всегда `04 00 00 00`   | Чтение без команды       |
| Отрицательное давление | `rawP < DMIN`            |
| Частые I2C ошибки      | Слишком частый опрос     |
| Busy всегда 1          | Busy-бит не используется |

---

## 10. Итог

* ✔️ Датчик **рабочий**
* ✔️ Протокол **командный**
* ❌ Не регистровый
* ⚠️ Частота **≈ 5 Гц**
* ✔️ Отлично подходит для:

  * давления воды
  * гидросистем
  * мониторинга труб
* ❌ Не подходит для быстрых процессов

---

## 11. Заключение

Если использовать этот датчик **правильно**, он работает стабильно и предсказуемо.  
Почти все проблемы, описанные в интернете, связаны не с браком, а с **неверным способом чтения данных**.

Этот пример создан именно для того, чтобы **не повторять эти ошибки**.

---

## Использование примера

1. Подключите датчик к ESP32 согласно схеме выше
2. Загрузите скетч `Minimal_for_all.ino` в Arduino IDE
3. Откройте Serial Monitor (115200 baud)
4. Вы должны увидеть вывод вида:

```
Pressure sensor start
P=0.000 bar | rawP=1641234 | rawT=40123
P=0.001 bar | rawP=1642345 | rawT=40125
...
```

---

## См. также

- [Работа с несколькими датчиками через PCA9548A](../pca9548a_multiplexer/README.md) — если нужно подключить несколько датчиков
- [Интеграция с Home Assistant через MQTT](../homeassistant_mqtt/README.md) — полнофункциональный боевой скетч
- [Документация производителя](../../docs/manufacturer/) — оригинальные файлы (содержат мало полезной информации)

